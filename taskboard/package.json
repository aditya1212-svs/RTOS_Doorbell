{
  "name": "taskboard",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-dependency shared task board for multi-agent sync, served on localhost:8081",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "nodemon": "^3.1.14"
  }
}
